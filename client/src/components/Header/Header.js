import React from "react";
import "./Header.css";

const Header = props => (
  <header className="header">
    <h1>Clicky Game!</h1>
    <h2>
      Test your memory! Click each picture once.
    </h2>
  </header>
);

export default Header;
